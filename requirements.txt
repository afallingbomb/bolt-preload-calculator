streamlit>=1.43.0,<2   # width="stretch"/"content" string API (st.dataframe/altair_chart/data_editor)
matplotlib>=3.9.0,<4   # boxplot(tick_labels=...) in the FE dashboard
reportlab>=4.0.0,<5
pandas>=2.0.0,<4
altair>=5.0.0,<7
numpy>=2.0.0,<3        # pandas 3.x; used by the vectorized bolt-group / FE-import paths
