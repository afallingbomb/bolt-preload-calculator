"""Single source of the application version, surfaced in the UI footer and the PDF
report so a given output is traceable to the model that produced it. Bump this
string when the calculation behaviour changes."""
__version__ = "1.1.0"
